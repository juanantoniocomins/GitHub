# 📘 Guía completa: 12 comandos de Git más utilizados

A continuación encontrarás una recopilación clara y práctica de los comandos más comunes de Git, acompañados de una tabla resumen para consultarlos rápidamente.

---

## 🧭 Tabla resumen de comandos

| Comando        | ¿Para qué sirve? |
|----------------|-------------------|
| `git init`     | Inicializa un repositorio local |
| `git clone`    | Copia un repositorio remoto |
| `git status`   | Muestra el estado del repositorio |
| `git add`      | Añade cambios al área de staging |
| `git commit`   | Guarda una versión de los cambios |
| `git push`     | Sube los commits al repositorio remoto |
| `git pull`     | Actualiza tu repositorio local desde remoto |
| `git branch`   | Gestiona ramas |
| `git checkout` | Cambia entre ramas o commits |
| `git merge`    | Fusiona ramas |
| `git diff`     | Muestra diferencias |
| `git log`      | Muestra el historial de commits |

---

